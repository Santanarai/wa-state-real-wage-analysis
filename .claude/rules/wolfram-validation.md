Wolfram MCP is available for independent cross-checking at validation points.

When validating a key computed value (CPI annual average, cumulative GWI, real-wage index), query WolframAlpha for the same data point and document the result in the validation cell. Format:

- Wolfram query: [exact query sent]
- Wolfram result: [value returned]
- Our computed value: [pandas result]
- Match: [yes/within tolerance/discrepancy]
- Reader verification: enter the query above at wolframalpha.com

For the compounding calculation, also compute the chained index using WolframLanguageEvaluator independently and document that both engines match.
